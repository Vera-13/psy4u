<div class="">
  <%= link_to doctors_path, class: "back-arrow" do %>
  <i class="fa-solid fa-arrow-left"></i>
  <% end %>
  <div class="box">
    <div class="box1">

      <div class="box-div">
        <% if @doctor.photo.attached? %>
            <%= cl_image_tag @doctor.photo.key,gravity: :faces, height: 200, width: 200, crop: :fill %>
          <% else %>
            <%= image_tag "icon.png" , height: 200, width: 200, crop: :fill %>
          <% end %>
        <div class="rate">
          <% unless @doctor.doctor_reviews.empty? %>
            <% array =  @doctor.doctor_reviews.map do |review| review.rating  end %>
                <%rat = array.sum / array.size %>
                <% rat.round.to_i.times do %> ⭐ <% end %>
          <% end %>
        </div>
      </div>

      <div class="box-div-name">
        <h2><%= @doctor.first_name %> <%= @doctor.last_name %></h2>
        <h4>Online Therapy & In Person Therapy</h4>
        <br>
        <h5>Specialization:</h5>
        <% @doctor.symptoms.each do |s|  %>
        <h4><%= s.name %></h4>
        <% end %>
      </div>

      <div class="box-div">
      <h5>Contacts:</h5>
      <h4> <i class="fa-solid fa-phone"></i> <%= @doctor.phone_nr %></h4>
      <h4> <i class="fa-solid fa-envelope"></i> <%= @doctor.email %></h4>
      <br>
        <h4>€<%= @doctor.price_session %>/session</h4>
      </div>
    </div>

    <div class="box2">
    <br>
    <h4>About <%= @doctor.first_name %>: </h4>
    <p> <h5><%= @doctor.about  %> </h5>
    </p>
    </div>

    <div class="box3">
      <%= simple_form_for([@doctor, @booking], url: doctor_bookings_path(@doctor)) do |f| %>
      <%= f.input :date, :label => "Choose a date",
          as: :string,
          input_html: { data: { controller: "datepicker" } }%>
      <%= f.submit "Book", class: "btn-message" %>
      <% end %>
    </div>

      <div class="future-map">
        <div style="width: 100%; height: 400px;"
        data-controller="map"
        data-map-marker-value="<%= @marker.to_json %>"
        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>

    <div class="box4">
    <div class="div-rev">
      <h5>Reviews</h5>
      <% if @doctor.doctor_bookings.where(patient: current_user, doctor: @doctor).any? %>
      <div><%= link_to "Review", new_doctor_review_path(@doctor), class: "btn-message"%></div>
      <% end %>
    </div>

      <%if @doctor.doctor_reviews %>
        <% @doctor.doctor_reviews.each do |review| %>
          <div class="box-div">
            <div>
            <%= review.comment %>
            <%  review.rating.to_i.times do  %>⭐<% end %>
            </div>
            <hr>
          </div>
        <% end %>
        <% else %>
          <h5>No reviews yet ... </h5>
        <% end %>
    </div>
  </div>
</div>
